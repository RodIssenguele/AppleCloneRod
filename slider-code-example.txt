<section class="slider">
  <!-- controls (hidden radios) -->
  <input checked type="radio" name="s" id="s1">
  <input type="radio" name="s" id="s2">
  <input type="radio" name="s" id="s3">
  <input type="radio" name="s" id="s4">

  <div class="viewport">
    <div class="track">
      <article class="slide">
        <img src="images/tv1.jpg" alt="Apple TV slide 1">
      </article>
      <article class="slide">
        <img src="images/tv2.jpg" alt="Apple TV slide 2">
      </article>
      <article class="slide">
        <img src="images/tv3.jpg" alt="Apple TV slide 3">
      </article>
      <article class="slide">
        <img src="images/tv4.jpg" alt="Apple TV slide 4">
      </article>
    </div>
  </div>

  <!-- dots -->
  <div class="dots">
    <label for="s1"></label>
    <label for="s2"></label>
    <label for="s3"></label>
    <label for="s4"></label>
  </div>

  <!-- arrows (optional) -->
  <div class="arrows">
    <div class="a a1">
      <label class="prev" for="s4">‹</label>
      <label class="next" for="s2">›</label>
    </div>
    <div class="a a2">
      <label class="prev" for="s1">‹</label>
      <label class="next" for="s3">›</label>
    </div>
    <div class="a a3">
      <label class="prev" for="s2">‹</label>
      <label class="next" for="s4">›</label>
    </div>
    <div class="a a4">
      <label class="prev" for="s3">‹</label>
      <label class="next" for="s1">›</label>
    </div>
  </div>
</section>


/* --- Apple-ish slider --- */
.slide-show{
  position: relative;
  width: min(1100px, 94vw);
  margin: 40px auto;
}

/* hide radios */
.slider > input{ position:absolute; opacity:0; pointer-events:none; }

.viewport{
  overflow:hidden;
  border-radius: 18px;
  background:#000;
}

.track{
  display:flex;
  width:400%;                 /* 4 slides */
  transition: transform .6s ease;
}

.slide{
  width:100%;
  flex: 0 0 100%;
  aspect-ratio: 16 / 9;       /* Apple TV style */
  position: relative;
}

.slide img{
  width:100%;
  height:100%;
  object-fit: cover;
  display:block;
}

/* move the track based on which radio is checked */
#s1:checked ~ .viewport .track{ transform: translateX(0%); }
#s2:checked ~ .viewport .track{ transform: translateX(-100%); }
#s3:checked ~ .viewport .track{ transform: translateX(-200%); }
#s4:checked ~ .viewport .track{ transform: translateX(-300%); }

/* dots */
.dots{
  display:flex;
  justify-content:center;
  gap:10px;
  padding: 14px 0 0;
}

.dots label{
  width:8px;
  height:8px;
  border-radius:999px;
  background: rgba(0,0,0,.25);
  cursor:pointer;
  transition: transform .2s ease, background .2s ease;
}

#s1:checked ~ .dots label[for="s1"],
#s2:checked ~ .dots label[for="s2"],
#s3:checked ~ .dots label[for="s3"],
#s4:checked ~ .dots label[for="s4"]{
  background: rgba(0,0,0,.55);
  transform: scale(1.2);
}

/* arrows */
.arrows{
  position:absolute;
  inset: 0;
  pointer-events:none; /* enable only labels */
}

.a{ display:none; }

#s1:checked ~ .arrows .a1,
#s2:checked ~ .arrows .a2,
#s3:checked ~ .arrows .a3,
#s4:checked ~ .arrows .a4{
  display:block;
}

.arrows label{
  pointer-events:auto;
  position:absolute;
  top:50%;
  transform: translateY(-50%);
  width:40px;
  height:40px;
  border-radius:999px;
  border: 1px solid rgba(255,255,255,.35);
  background: rgba(0,0,0,.35);
  color:#fff;
  display:grid;
  place-items:center;
  cursor:pointer;
  user-select:none;
  font-size: 26px;
  line-height: 1;
  backdrop-filter: blur(10px);
}

.arrows .prev{ left: 14px; }
.arrows .next{ right: 14px; }

/* mobile tweaks */
@media (max-width: 600px){
  .arrows label{ width:34px; height:34px; font-size:22px; }
}


Put your images in images/ and update the src="images/tv1.jpg" etc.

If you want 5 slides, duplicate one radio + one .slide, and update widths:

.track { width: 500%; }

Add #s5:checked rule and an extra dot + arrow block.

If you want it to auto-slide like Apple’s carousel, that does need JavaScript (or a CSS animation that auto-plays but can’t be properly paused/controlled). If you tell me which you prefer—click-only or auto + pause on hover—I’ll give you the best version.